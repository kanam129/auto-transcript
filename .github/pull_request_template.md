## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `cargo clippy --all-targets -- -D warnings` is clean
- [ ] `cargo test` passes
- [ ] `cargo run --release --example freshstart` passes
- [ ] `npm run build` passes

## If this touches latency or accuracy

- [ ] Numbers added to `BENCHMARK.md` (`examples/sim.rs` or `examples/bench.rs`)

<!-- "It feels faster" is genuinely hard to review. A table is easy. -->
